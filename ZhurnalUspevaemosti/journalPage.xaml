<Page x:Class="ZhurnalUspevaemosti.journalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ZhurnalUspevaemosti"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="journalPage" Loaded="Page_Loaded">

    <Grid Background="White" x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <ComboBox x:Name="classComboBox" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="130" HorizontalAlignment="Left" Margin="10,0,10,0"/>
            <ComboBox x:Name="subjectComboBox" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="130" HorizontalAlignment="Left" Margin="10,0,10,0">
                <ComboBoxItem Content="English"/>
                <ComboBoxItem Content="Math"/>
            </ComboBox>
            <Button x:Name="showButton" Content="Показать" Height="40" Margin="10,0,0,0" Click="showButton_Click"/>
        </StackPanel>
            
        <DataGrid Grid.Row="1" x:Name="dataGrid" AutoGenerateColumns="True" IsSynchronizedWithCurrentItem="False" ItemsSource="{Binding}" IsReadOnly="True"></DataGrid>
            
    </Grid>
</Page>
